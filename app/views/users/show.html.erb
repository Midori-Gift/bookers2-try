
<%= @books.each do |book| %>

  <div class="table">
    <table>
      <tr>
        <td>Title</td>
        <td>Opinion</td>
      </tr>
    </table>
  </div>
  <div>

  </div>

  <div>
    <%= link_to book_path(book.id) do %>
    <!-- <= attachment_image_tag image, :image > -->
    <p>ア馬場</p>
    <% end %>
    <%=link_to book.title, book_path(@book.id) %>
    <%= book.body %>
  </div>

<% end %>